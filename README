One Click Share
INCF Neuroimaging Datasharing Task Force
http://datasharing.incf.org/ni/Main_Page

Contents:

    README -- this file

    Client-side tools:

        one_click.das -- Deidentification script for DICOM Browser
        push -- simple push script

    Server-side tools:

        archive -- script to check archive data
        one_click -- python module to support scripts
        queue -- script to queue data for processing
        remove_broken_links -- script to remove broken QA links
        run_qc -- script to run QA
        update -- script (run by cron) for all updates

    XNAT customizations:

        xnat/ -- XNAT code (modified)
        XnatPipelineLauncher -- customized xnnppx launcher (modified)
